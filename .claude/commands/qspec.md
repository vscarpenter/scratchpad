Create or update `tasks/spec.md` for the requested non-trivial change.

Read `CLAUDE.md`, `CONTRIBUTING.md`, and the relevant source first. Capture the
goal, inputs and outputs, constraints, edge cases, anti-goals, acceptance
criteria, and test stubs. List assumptions explicitly. Stop for approval only
when the user has not already approved the design or spec.
