Implement the approved spec using one red/green/refactor cycle at a time.

Confirm each new behavior fails for the intended reason before changing the
implementation. Preserve Scratchpad's privacy, DOM-safety, no-build, and design
contracts. Record progress and deviations in `tasks/`, run the focused test
after every cycle, and commit each working logical unit.
